import { Table } from "react-bootstrap";

export default function Comments() {
    return (
        <Table striped bordered responsive>
            <tbody>
                <tr>
                    <td>Likes: {}</td>
                </tr>
            </tbody>
        </Table>
    );
}
